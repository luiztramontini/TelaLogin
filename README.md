# login-screen
Simple login screen, for testing and study purposes only. I created it as a base, a draft of what's to come. I included clear commands, such as the messages "Login successful!" when access is accepted, and "Incorrect login" when access is denied, in addition to using the asterisk "*" to hide the password.
